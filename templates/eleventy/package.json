{
  "name": "eleventy",
  "version": "1.0.0",
  "description": "Serverless Cloud Eleventy template",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "cloud",
    "test": "cloud test",
    "cloud:dev": "npx @11ty/eleventy --config=.eleventy.cjs --serve --incremental",
    "cloud:build": "npx @11ty/eleventy --config=.eleventy.cjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0",
    "@serverless/cloud": "^2.0.0",
    "http-proxy-middleware": "^2.0.4"
  },
  "serverless": {
    "app": "eleventy"
  }
}

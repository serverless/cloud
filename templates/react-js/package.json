{
  "name": "react-js",
  "version": "1.0.0",
  "description": "Serverless Cloud React.js template",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "cloud",
    "test": "cloud test",
    "build:frontend": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "@serverless/cloud": "^2.0.0"
  },
  "serverless": {
    "app": "react-js"
  }
}
